import React from "react";
import ReactDOM from "react-dom";
import Gallery from "./App";  // Import the Gallery component

ReactDOM.render(
  <React.StrictMode>
    <Gallery />
  </React.StrictMode>,
  document.getElementById("root")
);
